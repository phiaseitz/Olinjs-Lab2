// public/js/controllers/MainController.js
// main controller for rendering index.html page
// We can't take this out because it will break our header.

app.controller('MainController', function($scope) {
  
});